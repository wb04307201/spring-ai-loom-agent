loom:
  compile:
    mcp:
      basePath: .local/file
      mavenTimeoutMs: 600000
      dockerBuildTimeoutMs: 600000
      dockerRunTimeoutMs: 60000
      healthCheckMaxWaitMs: 60000
      healthCheckIntervalMs: 2000
      keepWorkspace: false

spring:
  ai:
    mcp:
      server:
        name: loom-compile-mcp
        version: @project.version@
        type: SYNC