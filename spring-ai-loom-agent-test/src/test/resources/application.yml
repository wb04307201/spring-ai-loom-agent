spring:
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}
      chat:
        options:
          model: qwen3.7-plus
          multi_model: true
          enable_thinking: true
      embedding:
        options:
          model: text-embedding-v4
server:
  port: 8089
logging:
  level:
    org:
      springframework:
        ai: DEBUG
