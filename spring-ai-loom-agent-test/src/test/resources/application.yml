spring:
  ai:
    anthropic:
      api-key: ${NEW_API_KEY}
      base-url: http://123.56.128.32:3000
      chat:
        options:
          model: qwen3.7-plus
server:
  port: 8080
logging:
  level:
    org:
      springframework:
        ai: DEBUG
