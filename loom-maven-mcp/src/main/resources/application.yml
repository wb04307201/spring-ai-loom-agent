loom:
  maven:
    mcp:
      basePath: .local/file
      maxOutputLines: 200
      defaultTimeoutMs: 300000

spring:
  ai:
    mcp:
      server:
        name: loom-maven-mcp
        version: 1.1.34
        type: SYNC
