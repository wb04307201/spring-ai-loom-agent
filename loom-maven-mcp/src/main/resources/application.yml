loom:
  maven:
    mcp:
      basePath: ${user.home}/.loom/mcp
      maxOutputLines: 200
      defaultTimeoutMs: 300000

spring:
  ai:
    mcp:
      server:
        name: loom-maven-mcp
        version: @project.version@
        type: SYNC