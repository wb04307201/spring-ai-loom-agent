loom:
  git:
    mcp:
      basePath: ${user.home}/.loom/file
      gitUsername: ""
      gitToken: ""
      remoteTimeoutSeconds: 60

spring:
  ai:
    mcp:
      server:
        name: loom-git-mcp
        version: @project.version@
        type: SYNC