loom:
  git:
    mcp:
      basePath: .local/file
      gitUsername: ""
      gitToken: ""
      remoteTimeoutSeconds: 60

spring:
  ai:
    mcp:
      server:
        name: loom-git-mcp
        version: @project.version@
        type: SYNC